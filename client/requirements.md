## Packages
framer-motion | For smooth transitions between landing and search states
clsx | For conditional class names
tailwind-merge | For merging tailwind classes utility

## Notes
- Search state transition happens on client-side when query string exists
- Using /api/content for data fetching
- Images will use placeholders if actual URLs are missing
